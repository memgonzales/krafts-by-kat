{{>navbar userFlag = userFlag adminFlag = adminFlag username = username}}

{{!-- HBS Conditions for index --}}
{{#if userFlag}}
	{{#if adminFlag}}
		{{> home}}
	{{else}}
		{{> home}}
	{{/if}}
{{else}}
	{{> landing}}
{{/if}}