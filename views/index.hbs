{{>navbar userFlag = userFlag adminFlag = adminFlag}}

{{!-- HBS Conditions for index --}}
{{#if userFlag}}
	{{#if adminFlag}}
		{{> admin-home}}
	{{else}}
		{{> home}}
	{{/if}}
{{else}}
	{{> landing}}
{{/if}}